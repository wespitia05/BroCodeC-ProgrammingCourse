#include <iostream>

int main() {
    // if statements = do something if a condition is true
    //                 if not, then don't do it

    int age;

    std::cout << "enter your age: ";
    std::cin << age;

    return 0;
}